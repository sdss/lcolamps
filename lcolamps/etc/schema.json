{
  "$schema": "http://json-schema.org/draft-07/schema",
  "type": "object",
  "properties": {
    "SQM": { "type": "string", "enum": ["ON", "OFF", "UNKNOWN"] },
    "FF": { "type": "string", "enum": ["ON", "OFF", "UNKNOWN"] },
    "Lsr": { "type": "string", "enum": ["ON", "OFF", "UNKNOWN"] },
    "HgAr": { "type": "string", "enum": ["ON", "OFF", "UNKNOWN"] },
    "Ne": { "type": "string", "enum": ["ON", "OFF", "UNKNOWN"] }
  },
  "additionalProperties": false
}
